---
// Home EN (locale "en" → "/en/"). Mismo contenido que ES; el copy/datos
// salen por Astro.currentLocale (que aquí resuelve a "en").
import Layout from "../../layouts/Layout.astro";
import Home from "../../components/Home.astro";
---

<Layout>
  <Home />
</Layout>
