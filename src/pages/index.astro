---
// Home ES (locale default → "/"). El copy/datos salen por Astro.currentLocale.
// Pendientes y roadmap: ver PROGRESS.md en la raíz del repo.
import Layout from "../layouts/Layout.astro";
import Home from "../components/Home.astro";
---

<Layout>
  <Home />
</Layout>
